name: GitHub Actions Demo
on: [push]
jobs:
  Laba1:
    runs-on: ubuntu-latest

    steps:
    - run: python3 --version
    - run: ls -a ${{ github.workspace }}
    - run: python -m unittest ${{ github.workspace }}/laba1/tests/tests.py
